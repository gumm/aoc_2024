import {linesToArray, inspect, readInput, } from "../utils.js";

// Parse Input
let task1 = 0;
let task2 = 0;
const lines = linesToArray(readInput('aoc_8_0.txt'));
inspect(lines);


inspect(task1);
inspect(task2);
