import {inspect, linesToArray, readInput,} from "../utils.js";

let task1 = 0
let task2 = 0
const matrix = linesToArray(readInput('aoc_9_e.txt')).map(e => e.split(''));
inspect(matrix);

inspect(task1);
inspect(task2);
